# 本机直接调试网关时可设置（NanoClaw 使用时由 .env 的 ANTHROPIC_API_KEY 经 proxy 注入，无需在此填写）
# DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxx

# 可选：默认通义模型（当 Anthropic 模型名未在 MODEL_MAP 中配置时使用）
# DASHSCOPE_MODEL=qwen-plus

# 可选：DashScope 兼容模式 base URL（一般无需改）
# DASHSCOPE_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
