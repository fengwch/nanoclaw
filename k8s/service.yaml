apiVersion: v1
kind: Service
metadata:
  name: nanoclaw
  namespace: nanoclaw
spec:
  selector:
    app: nanoclaw
  ports:
    - name: credential-proxy
      port: 3001
      targetPort: 3001
    - name: http
      port: 3000
      targetPort: 3000
